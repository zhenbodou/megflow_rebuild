[package]
name = "flow-rs"
version = "0.1.0"
edition = "2021"

[workspace]
exclude = ["message"]

[dependencies]
flow-message = { path = "message" }
tokio = { version = "1", features = ["rt", "sync", "macros", "time"] }

[dev-dependencies]
futures-util = "0.3"
