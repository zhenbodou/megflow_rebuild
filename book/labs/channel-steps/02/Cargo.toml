[package]
name = "flow-rs"
version = "0.1.0"
edition = "2021"

[workspace]
exclude = ["message"]

[dependencies]
tokio = { version = "1", features = ["sync", "rt", "macros"] }
flow-message = { path = "message" }
